{
  "design_name": "cdc_twoclock",
  "expected_outcome": "fail",
  "expected_outcome_reason": "Negative control for the CDC signoff gate (see src/cdc_twoclock.v). OpenLane's base.sdc constrains only the first CLOCK_PORT, so every run is unverified for clk_b by construction; an OPEN case here is the gate working, not the design being stuck. self_improve.py keeps it out of the review backlog for that reason.",
  "targets": {
    "max_core_utilization": 0.75,
    "max_ir_drop_pct": 5.0
  },
  "candidates": [
    {
      "tag": "cand-baseline",
      "overrides": {}
    }
  ],
  "max_iterations": 1,
  "sweeps": [
    {
      "param": "CLOCK_PERIOD",
      "values": [
        6,
        8,
        10,
        14,
        20
      ],
      "tag_prefix": "sweep-clk"
    }
  ]
}
